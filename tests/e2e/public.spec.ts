import {test,expect} from "@playwright/test";test("public landing and staff login are accessible",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Safer decisions/i})).toBeVisible();await page.getByRole("link",{name:/staff sign in/i}).first().click();await expect(page.getByRole("heading",{name:/secure staff sign in/i})).toBeVisible();await expect(page.getByLabel("Email")).toBeVisible();await expect(page.getByLabel("Password")).toBeVisible()});
