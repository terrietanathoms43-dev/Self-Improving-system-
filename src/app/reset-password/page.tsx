import { Button,Card,Input } from "@/components/ui";import { updatePassword } from "./actions";
export default async function Reset({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="grid min-h-screen place-items-center bg-slate-100 p-4"><Card className="w-full max-w-md"><h1 className="text-2xl font-bold">Choose a new password</h1>{error?<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-800">{error}</p>:null}<form action={updatePassword} className="mt-6 space-y-4"><Input name="password" type="password" minLength={12} autoComplete="new-password" placeholder="New password" required/><Input name="confirm" type="password" minLength={12} autoComplete="new-password" placeholder="Confirm password" required/><Button className="w-full">Update password</Button></form></Card></main>}
