import { Button,Card,Input } from "@/components/ui";import { requestReset } from "./actions";
export default async function Forgot({searchParams}:{searchParams:Promise<{sent?:string}>}){const {sent}=await searchParams;return <main className="grid min-h-screen place-items-center bg-slate-100 p-4"><Card className="w-full max-w-md"><h1 className="text-2xl font-bold">Reset password</h1><p className="mt-2 text-sm text-slate-600">Enter your authorized staff email. For privacy, the response is the same whether an account exists or not.</p>{sent?<p role="status" className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">If the account exists, a secure reset link has been sent.</p>:<form action={requestReset} className="mt-6 space-y-4"><Input name="email" type="email" autoComplete="email" required/><Button className="w-full">Send reset link</Button></form>}</Card></main>}
