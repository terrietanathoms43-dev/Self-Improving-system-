import {requireActor} from "@/lib/auth";import {createClient} from "@/lib/supabase/server";import {Card,Badge} from "@/components/ui";
export default async function Governance(){await requireActor(['human_oversight_committee','admin']);const s=await createClient();const [{data:proposals},{data:versions},{data:evaluations}]=await Promise.all([s.from('policy_proposals').select('*').order('created_at',{ascending:false}),s.from('model_versions').select('*').order('created_at',{ascending:false}),s.from('model_evaluations').select('*,model_versions(version)').order('created_at',{ascending:false})]);return <><h1 className="text-3xl font-bold">Rules and model governance</h1><p className="mt-2 text-slate-600">Draft → evaluate → committee approval → manual activation. Automatic retraining and deployment are disabled.</p><div className="mt-6 grid gap-6 xl:grid-cols-2"><Card><h2 className="font-semibold">Policy-improvement queue</h2><div className="mt-4 space-y-3">{(proposals??[]).map(p=><div key={p.id} className="rounded-lg border p-4"><Badge>{p.status}</Badge><h3 className="mt-2 font-semibold">{p.title}</h3><p className="mt-1 text-sm text-slate-600">{p.problem_statement}</p></div>)}{!proposals?.length?<p className="text-sm text-slate-500">No proposals awaiting review.</p>:null}</div></Card><Card><h2 className="font-semibold">Version history and rollback</h2><div className="mt-4 space-y-3">{(versions??[]).map(v=><div key={v.id} className="flex items-center justify-between rounded-lg border p-4"><div><p className="font-mono font-semibold">{v.version}</p><p className="text-sm text-slate-500">{v.change_summary}</p></div><Badge tone={v.status==='active'?'success':'default'}>{v.status}</Badge></div>)}{!versions?.length?<p className="text-sm text-slate-500">No version has been configured.</p>:null}</div></Card></div><Card className="mt-6"><h2 className="font-semibold">Evaluation and regression results</h2><div className="mt-4 grid gap-3 md:grid-cols-3">{(evaluations??[]).map(e=><div key={e.id} className="rounded-lg bg-slate-50 p-4"><Badge tone={e.status==='passed'?'success':e.status==='failed'?'danger':'default'}>{e.status}</Badge><p className="mt-2 font-semibold">{(e.model_versions as unknown as {version:string})?.version}</p><p className="text-sm text-slate-500">{e.dataset_name} · {e.dataset_version}</p></div>)}</div></Card></>}
