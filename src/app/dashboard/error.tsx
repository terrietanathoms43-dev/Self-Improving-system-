"use client";import { useEffect } from "react";import { Button,Card } from "@/components/ui";
export default function DashboardError({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error("CareBridge dashboard error",{digest:error.digest})},[error]);return <Card><h1 className="text-2xl font-bold">This operation could not be completed</h1><p className="mt-2 text-slate-600">No sensitive details were exposed. Try again or contact an administrator with reference {error.digest??"unavailable"}.</p><Button className="mt-4" onClick={reset}>Try again</Button></Card>}
