import { createHash } from "node:crypto";
import { headers } from "next/headers";
import { createAdminClient } from "@/lib/supabase/server";
export async function enforceRateLimit(action:string,limit:number,windowSeconds:number){const h=await headers();const forwarded=h.get("x-forwarded-for")?.split(",")[0]?.trim()||"unknown";const salt=process.env.RATE_LIMIT_SALT;if(!salt)throw new Error("Rate limiting is not configured");const key=createHash("sha256").update(`${salt}:${forwarded}`).digest("hex");const admin=createAdminClient();const {data,error}=await admin.rpc("cbg_take_rate_limit",{p_key_hash:key,p_action:action,p_limit:limit,p_window_seconds:windowSeconds});if(error)throw new Error("Rate limiter unavailable");if(!data)throw new Error("Too many attempts. Please wait and try again.")}
export async function logSafeError(route:string,operation:string,error:unknown,actorId?:string){const admin=createAdminClient();const code=error&&typeof error==="object"&&"code" in error?String(error.code):"UNEXPECTED";await admin.from("cbg_error_events").insert({actor_id:actorId??null,route,operation,error_code:code,safe_message:"The operation failed",metadata:{runtime:"nextjs"}})}
