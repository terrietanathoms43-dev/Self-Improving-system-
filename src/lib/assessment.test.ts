import {describe,expect,it} from "vitest";import {assessApplication,canCreateFinalDecision} from "./assessment";import type {AssessmentInput} from "@/types/database";
const base:AssessmentInput={medicalUrgency:20,financialHardship:15,accessBarriers:10,unmetNeed:10,vulnerability:5,supportGap:3,missingDocuments:false,missedAppointments:false,rural:false,transportDifficulty:false,disability:false,caregiving:false,urgent:false,sensitive:false,appealed:false,conflicting:false,unusual:false,lowConfidence:false,child:false,elderly:false,pregnant:false};
describe("transparent assessment",()=>{it("sums the six bounded components",()=>{const result=assessApplication(base);expect(result.score).toBe(63);expect(result.category).toBe("high");expect(result.requiresHumanReview).toBe(true)});it("routes missing documents for follow-up without rejection",()=>{const result=assessApplication({...base,missingDocuments:true});expect(result.missingInformation[0]).toContain("follow-up");expect(result.recommendedAction).not.toContain("reject");expect(result.confidence).toBeLessThan(100)});it("treats transport and missed appointments as fairness context",()=>{const result=assessApplication({...base,rural:true,missedAppointments:true});expect(result.fairnessWarnings).toHaveLength(2);expect(result.fairnessWarnings.join(" ")).toContain("must not be treated as misconduct")});it("blocks every AI-created final decision",()=>{expect(canCreateFinalDecision("ai","approve")).toBe(false);expect(canCreateFinalDecision("ai","reject")).toBe(false);expect(canCreateFinalDecision("human","reject")).toBe(true)});it("rejects out-of-range component scores",()=>{expect(()=>assessApplication({...base,medicalUrgency:31})).toThrow()})});
