import {describe,expect,it} from "vitest";import {categoryForScore,evaluateCase,summarizeEvaluation} from "./evaluation";
const input={medicalUrgency:20,financialHardship:15,accessBarriers:10,unmetNeed:10,vulnerability:5,supportGap:2,missingDocuments:false,missedAppointments:false,rural:false,transportDifficulty:false,disability:false,caregiving:false,urgent:false,sensitive:false,appealed:false,conflicting:false,unusual:false,lowConfidence:false,child:false,elderly:false,pregnant:false};
describe("evaluation engine",()=>{it("uses versioned thresholds",()=>expect(categoryForScore(62,{high:65})).toBe("moderate"));it("detects a regression",()=>expect(evaluateCase(input,"high",{high:60},{high:65}).regression).toBe(true));it("does not mutate production rules",()=>{const rules={high:65};evaluateCase(input,"high",{},rules);expect(rules).toEqual({high:65})});it("fails poor accuracy even with zero regressions",()=>{const results=Array.from({length:20},(_,i)=>({expected:"critical",before:"standard",after:"standard",subgroup:{parish:i<10?"A":"B"}}));expect(summarizeEvaluation(results).criteria_met).toBe(false)});it("requires subgroup evidence",()=>{const results=Array.from({length:20},()=>({expected:"high",before:"high",after:"high",subgroup:{parish:"A"}}));expect(summarizeEvaluation(results).gates.subgroupFairness).toBe(false)})});
